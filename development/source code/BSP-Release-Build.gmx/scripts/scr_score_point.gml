//arg0 points
//only use with pug var.

/*

if pug >= 0{
obj_scored += 1;
i13 = floor(argument0/obj_scored);
if i13 > 0{
with pug{
g.pug_score[player_number] += other.i13;
opt = instance_create(other.x,other.y-64,obj_score_popup);
opt.points = "+"+string(other.i13);
}}}


*/
